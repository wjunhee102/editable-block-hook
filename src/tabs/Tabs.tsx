import React from 'react';

/**
 * tabs 기능
 * 1. 탭을 좌우로 분리 
 * 2. 탭을 상하로 분리
 * 3. 탭을 다른 영역으로 이동 (drag & drop)
 * 4. 탭 추가
 * 5. 택 삭제
 */


const Tabs: React.FC = () => {
  return (
    <div>

    </div>
  );
}

export default Tabs;